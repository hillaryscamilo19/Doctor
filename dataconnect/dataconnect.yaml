specVersion: "v1"
serviceId: "doctor"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "doctor-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
